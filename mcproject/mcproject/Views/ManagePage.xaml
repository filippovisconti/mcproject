<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:vm="clr-namespace:mcproject.Views" 
             xmlns:vm1="clr-namespace:mcproject.ViewModels"
             x:Class="mcproject.Views.ManagePage">

    <ContentPage.BindingContext>
        <vm1:ManageViewModel/>
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Manage Page"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />


            <Button 
                
                IsVisible="{Binding notLoggedIn}" 
                Text="Sign in" 
                Command="{Binding GoToHomePage}"
                
                    />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>